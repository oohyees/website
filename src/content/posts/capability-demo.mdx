---
author: oohyees
pubDatetime: 2026-05-27T06:00:00Z
modDatetime: 2026-05-28T02:00:00Z
title: Astro 博客能力边界探索
slug: capability-demo
featured: true
draft: false
tags:
  - tech
  - Astro
  - MDX
  - 演示
description: "全方位展示 Astro 博客的全部能力：自定义组件、React 交互、数学公式、Mermaid 图表、代码增强、Directive 短码、评论区等。"
---

import Callout from '@/components/mdx/Callout.astro';
import Steps from '@/components/mdx/Steps.astro';
import Step from '@/components/mdx/Step.astro';
import Collapsible from '@/components/mdx/Collapsible.astro';
import CodeCompare from '@/components/mdx/CodeCompare.astro';
import ResponsiveTable from '@/components/ResponsiveTable.astro';
import LinkButton from '@/components/LinkButton.astro';
import Tag from '@/components/Tag.astro';
import Tabs from '@/components/mdx/Tabs.tsx';
import Counter from '@/components/mdx/Counter.tsx';
import Mermaid from '@/components/mdx/Mermaid.tsx';

本文是一篇能力边界演示，尽可能展示 Astro 博客生态中的全部可用功能。

## 1. Callout 提示块

五种内置样式：

<Callout type="info" title="信息提示">
这是一个信息类型的提示块。适合放一些补充说明、背景知识等内容。
</Callout>

<Callout type="tip" title="小技巧">
你可以用 `pnpm dev` 启动开发服务器，修改内容会自动热更新。
</Callout>

<Callout type="warning" title="注意事项">
构建前请确认所有文章 `draft: false` 已设置正确。
</Callout>

<Callout type="success" title="部署完成">
部署成功！你的网站已上线，可以通过 Vercel 域名访问。
</Callout>

<Callout type="error">
Vercel 构建失败：缺少 `zh` 语言文件。
</Callout>

## 2. React 交互组件

### Tabs 标签页

<Tabs
  client:only="react"
  tabs={[
    { 
      label: "TypeScript",
      content: `<pre><code>interface User {
  id: number
  name: string
  email: string
}

const user: User = {
  id: 1,
  name: "oohyees",
  email: "hi@example.com",
}</code></pre>`
    },
    { 
      label: "Python",
      content: `<pre><code>from dataclasses import dataclass

@dataclass
class User:
    id: int
    name: str
    email: str

user = User(id=1, name="oohyees", email="hi@example.com")</code></pre>`
    },
    { 
      label: "Go",
      content: `<pre><code>type User struct {
    ID    int
    Name  string
    Email string
}

user := User{
    ID:    1,
    Name:  "oohyees",
    Email: "hi@example.com",
}</code></pre>`
    },
  ]}
/>

### Counter 计数器

<div style="display: flex; gap: 1rem; flex-wrap: wrap;">
  <Counter client:only="react" initial={0} label="点赞数" />
  <Counter client:only="react" initial={42} label="访问量" />
</div>

### Mermaid 流程图

<Mermaid client:only="react" code={`flowchart TD
    A[写 Markdown 文章] --> B{git push}
    B --> C[GitHub 仓库]
    C --> D[Vercel 自动检测]
    D --> E[构建 & 部署]
    E --> F[全球 CDN 上线]
    F --> G[用户访问]
    G -->|有新内容| B
    E -->|构建失败| H[查看日志]
    H --> A`} />

架构图：

<Mermaid client:only="react" code={`graph LR
    subgraph 内容层
        MDX[MDX 文章]
        MD[Markdown 文章]
    end
    subgraph 框架层
        ASTRO[Astro]
        REACT[React Islands]
    end
    subgraph 部署层
        VERCEL[Vercel]
        CDN[Global CDN]
    end

    MDX --> ASTRO
    MD --> ASTRO
    ASTRO --> REACT
    ASTRO --> VERCEL
    VERCEL --> CDN`} />

## 3. 自带组件：LinkButton 和 Tag

<Tag tag="演示" tagName="演示" />

<br/>

<LinkButton href="https://github.com/oohyees/oohyees.github.io" class="text-sm">
  查看本站源码
</LinkButton>

<Callout type="info">
以上 `Tag` 和 `LinkButton` 是 AstroPaper 内置组件，所有 `.mdx` 文章都可以直接 import 使用。注意 `Tag` 的 `tag` prop 是 URL slug，`tagName` 是显示文本。
</Callout>

## 4. 步骤引导

<Steps>
  <Step>
    **安装 Node.js 和 pnpm**。确保 `node -v` 和 `pnpm -v` 能正确输出版本号。
  </Step>
  <Step>
    **克隆模板**。从 GitHub 选择一个 Astro 博客模板，Fork 后克隆到本地。
  </Step>
  <Step>
    **安装依赖**。在项目目录运行 `pnpm install`。
  </Step>
  <Step>
    **本地预览**。运行 `pnpm dev`，浏览器打开 `http://localhost:4321/`。
  </Step>
  <Step>
    **部署上线**。代码推到 GitHub，Vercel 一键导入，每次 push 自动部署。
  </Step>
</Steps>

## 5. 可折叠内容

<Collapsible summary="点击展开：Astro 的核心特色">

- **Islands Architecture**：只有必要的交互组件才发送 JS 到客户端
- **零 JS 默认**：纯内容页面完全不加载 JavaScript
- **多框架支持**：同时使用 React、Vue、Svelte 组件
- **Content Collections**：内置类型安全的内容管理系统

</Collapsible>

## 6. 响应式表格

<ResponsiveTable variant="striped">
| 特性 | 普通 Markdown | MDX | Astro 组件 |
|------|:----------:|:---:|:---------:|
| 基础排版 | ✅ | ✅ | — |
| 代码高亮 | ✅ | ✅ | — |
| LaTeX 公式 | ✅ | ✅ | — |
| 嵌入组件 | ❌ | ✅ | — |
| 交互功能 | ❌ | ✅ | ✅ |
| 流程图 | ❌ | ❌ | ✅ |
</ResponsiveTable>

<ResponsiveTable variant="striped-minimal">
| 框架 | Astro 支持 | 需要 adapter |
|------|:--------:|:----------:|
| React | ✅ | 否 |
| Vue | ✅ | 否 |
| Svelte | ✅ | 否 |
| Solid | ✅ | 否 |
| Preact | ✅ | 否 |
</ResponsiveTable>

## 7. 代码增强

### 文件名标注

```ts file="src/utils/slugify.ts"
export function slugifyStr(str: string): string {
  return str
    .toLowerCase()
    .replace(/\s+/g, "-")
    .replace(/[^\w-]+/g, "");
}
```

### 代码 Diff

```ts diff
function greet(name: string): string {
- return "Hello, " + name + "!";
+ return `Hello, ${name}!`;
}
```

### 行高亮

```ts "useState"
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);
  return (
    <button onClick={() => setCount(count + 1)}>
      Clicked {count} times
    </button>
  );
}
```

## 8. 代码对比

<CodeCompare titleA="JS (旧版)" titleB="TS (新版)">
  <Fragment slot="a">

```js
function calculateTotal(items) {
  let total = 0;
  for (let i = 0; i < items.length; i++) {
    total += items[i].price;
  }
  return total;
}
```

  </Fragment>
  <Fragment slot="b">

```ts
function calculateTotal(items: Item[]): number {
  return items.reduce((total, item) => total + item.price, 0);
}
```

  </Fragment>
</CodeCompare>

## 9. 数学公式

行内公式：动能 $E_k = \frac{1}{2}mv^2$，欧拉公式 $e^{i\pi} + 1 = 0$。

块级公式——正态分布概率密度函数：

$$
f(x) = \frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}
$$

矩阵乘法：

$$
\begin{bmatrix} a & b \\ c & d \end{bmatrix}
\begin{bmatrix} x \\ y \end{bmatrix}
=
\begin{bmatrix} ax + by \\ cx + dy \end{bmatrix}
$$

## 10. 外部链接自动处理

由 `rehype-external-links` 自动添加 `target="_blank"` 和 `rel="noopener noreferrer"`：

- [Astro 官方文档](https://docs.astro.build/)
- [Vercel 平台](https://vercel.com/)
- [GitHub](https://github.com/oohyees/oohyees.github.io)

内部链接（如 [回到首页](/)）保持不变。

## 11. 混合使用

<Callout type="tip">

### 组件嵌套

你可以把 **LaTeX**、**代码块** 甚至其他元素嵌套在 Callout 里：

```ts
const magic = Math.E ** (Math.PI * Math.i) + 1; // ≈ 0
```

欧拉恒等式：$e^{i\pi} + 1 = 0$

</Callout>

---

## 能力总览

| 类别 | 已实现功能 |
|------|----------|
| **Astro 组件** | Callout (5 种)、Steps/Step、Collapsible、CodeCompare、ResponsiveTable (3 种) |
| **内置组件** | LinkButton、Tag |
| **React 交互** | Tabs、Counter、Mermaid 流程图/架构图 |
| **数学公式** | LaTeX 行内/块级，KaTeX 渲染 |
| **代码增强** | Shiki 双主题、Diff、Highlight、文件名标注 |
| **链接处理** | 外链 target="_blank"、标题锚点 |
| **阅读时间** | 自动计算，显示在文章日期旁 |
| **评论区** | Giscus（GitHub Discussions 驱动） |
